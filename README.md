# The Road Rescue Guys — Next.js

Converted from the supplied Stitch HTML into a Next.js App Router project.

## Run
```bash
npm install
npm run dev
```

Routes: `/`, `/services`, `/about-us`, `/contact-us`.
